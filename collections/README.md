# Collections demos

This folder contains Java programs demonstrating:

- `ArrayListDemo.java` — basic CRUD operations
- `HashMapDemo.java` — keySet(), values(), entrySet()
- `HashSetDemo.java` — duplicate handling


Run using:  
Open in Eclipse → Run As → Java Application.
